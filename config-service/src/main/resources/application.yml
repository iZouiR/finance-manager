spring:
  application:
    name: config
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/config/{application}
  profiles:
    active: native

server:
  port: ${CONFIG_PORT}